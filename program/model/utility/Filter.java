package program.model.utility;


public class Filter {
    //Currently is the old version of filter, will be redone in future implementation


//    public static ArrayList<TaggedObject> tagFilter (ArrayList<String> tags, ArrayList<TaggedObject> items) {
//        ArrayList<TaggedObject> res = new ArrayList<>();
//        for (TaggedObject item: items)
//            for (String tag: tags)
//                if (item.tags.containsAll(tags)) res.add(item);
//        return res;
//    }
}